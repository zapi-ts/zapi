{
  "name": "zapi-monorepo",
  "version": "0.1.0-beta.3",
  "private": true,
  "description": "Zero to API in seconds",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "turbo": "^2.6.0",
    "typescript": "^5.7.0",
    "tsup": "^8.5.0",
    "vitest": "^3.0.0"
  },
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
