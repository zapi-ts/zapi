{
  "name": "create-zapi-x",
  "version": "0.1.0-beta.2",
  "description": "Create a new zapi project - Zero to API in seconds",
  "type": "module",
  "bin": {
    "create-zapi-x": "./dist/index.js"
  },
  "main": "dist/index.js",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --clean --shims",
    "dev": "tsup src/index.ts --format esm --watch --shims",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/prompts": "^2.4.9",
    "tsup": "^8.5.0",
    "typescript": "^5.7.0"
  },
  "keywords": ["zapi", "create", "scaffold", "cli", "api", "rest", "prisma"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/zapidev/zapi",
    "directory": "packages/create-zapi"
  }
}
